// D9: Instant loading skeleton for the public QR menu (customer-facing).
import { MenuSkeleton } from '@/components/ui/skeleton';

export default function MenuLoading() {
  return <MenuSkeleton />;
}
