# نظام التصميم — دكان (Dokan)
### Enterprise SaaS Design System · v1.0

**الهوية:** أداة مؤسسية مدفوعة، لا منتج استهلاكي. القياس: Shopify Plus / Salla Enterprise / أنظمة الكاشير الحديثة (Toast, Square).
**المبادئ الحاكمة:** رياضي · صارم · هادئ · بلا طاقة "ستارت أب" · RTL أولاً · Cairo فقط · لا Dark Mode.

---

## 0. فلسفة التصميم

1. **الانضباط قبل الشخصية.** كل قيمة (لون، مسافة، نصف قطر) مشتقة من نظام رقمي ثابت — لا قيم عشوائية أبدًا.
2. **الحياد اللوني.** الرمادي البارد (Slate) هو الأساس؛ اللون (Indigo) يُستخدم فقط للفعل والتركيز، لا للزخرفة.
3. **الكثافة المهنية.** حجم الخط الأساسي 14px — مناسب لواجهة عمل يستخدمها الموظف 8 ساعات، وليس صفحة تسويقية.
4. **الحدود بدل الظلال.** الفصل البصري الأساسي عبر `border` 1px، والظل للتموضع الطبقي فقط (قوائم منسدلة، نوافذ منبثقة).
5. **RTL حقيقي لا معكوس.** خصائص منطقية (`margin-inline-start` لا `margin-left`)، لا نسخة "مترجمة" من تصميم LTR.
6. **الأرقام لاتينية دائمًا.** المبالغ، أرقام الطلبات، التواريخ تُكتب بأرقام غربية (0-9) داخل نص عربي — معيار الأنظمة المالية الخليجية (STC Pay, مصارف البحرين).

---

## 1. الألوان (Color Tokens)

نظام أحادي اللون فاتح فقط. لا `.dark`. التباين محسوب لمعيار WCAG AA كحد أدنى على كل الأزواج المستخدمة للنص.

### 1.1 العلامة التجارية

| الدور | Hex | متغير CSS | الاستخدام |
|---|---|---|---|
| Primary | `#4F46E5` | `--color-primary` | أزرار أساسية، روابط فعّالة، حالة التحديد |
| Primary Hover | `#4338CA` | `--color-primary-hover` | تحويم/ضغط الأزرار الأساسية |
| Primary Active | `#3730A3` | `--color-primary-active` | حالة الضغط (mousedown) |
| Primary Tint | `#EEF2FF` | `--color-primary-tint` | خلفية شارات/تحديد صف في جدول |
| Primary Tint Strong | `#E0E7FF` | `--color-primary-tint-strong` | خلفية أيقونة نشطة في الشريط الجانبي |

**قاعدة صارمة:** Indigo لا يظهر إلا في: زر أساسي واحد لكل شاشة (CTA)، حالة تحديد/تركيز، روابط نصية قابلة للنقر. لا يُستخدم كخلفية زخرفية أو Gradient.

### 1.2 المحايد (Slate)

| الدور | Hex | متغير CSS |
|---|---|---|
| Background | `#F8FAFC` | `--color-bg` |
| Surface (بطاقات/لوحات) | `#FFFFFF` | `--color-surface` |
| Surface Sunken (مناطق ثانوية) | `#F1F5F9` | `--color-surface-sunken` |
| Border | `#E2E8F0` | `--color-border` |
| Border Strong | `#CBD5E1` | `--color-border-strong` |
| Text Primary | `#0F172A` | `--color-text` |
| Text Secondary | `#475569` | `--color-text-secondary` |
| Text Muted | `#94A3B8` | `--color-text-muted` |
| Text Disabled | `#CBD5E1` | `--color-text-disabled` |

### 1.3 الدلالية (منزوعة التشبع عمدًا — لا ألوان "ألعاب")

| الدور | Hex | Tint | متغير |
|---|---|---|---|
| Success | `#059669` | `#ECFDF5` | `--color-success` / `--color-success-tint` |
| Warning | `#D97706` | `#FFFBEB` | `--color-warning` / `--color-warning-tint` |
| Danger | `#DC2626` | `#FEF2F2` | `--color-danger` / `--color-danger-tint` |
| Info | `#0284C7` | `#F0F9FF` | `--color-info` / `--color-info-tint` |

كل لون دلالي له نسخة `-hover` أغمق بمقدار خطوة واحدة على مقياس Tailwind (600→700) لاستخدامها في الأزرار الثانوية الدلالية (مثال: زر "إلغاء الطلب" بلون Danger).

---

## 2. الطباعة (Typography)

**الخط الوحيد: Cairo.** لا خطوط ثانوية، لا خط عرض منفصل. الانضباط يعني عائلة واحدة بأوزان متعددة.

الأوزان المستخدمة: `400` (نص)، `500` (تسميات/UI)، `600` (عناوين فرعية/تشديد)، `700` (عناوين)، `800` (أرقام KPI فقط).

### 2.1 المقياس (Base 14px — كثافة تطبيق عمل، ليست صفحة تسويقية)

| Token | Size / Line | Weight | الاستخدام |
|---|---|---|---|
| `--text-xs` | 12px / 16px | 500 | تسميات، وسوم، تلميحات الجدول |
| `--text-sm` | 13px / 20px | 400 | نص ثانوي، خلايا جدول كثيفة |
| `--text-base` | 14px / 20px | 400 | نص الواجهة الافتراضي |
| `--text-md` | 16px / 24px | 500 | نص مؤكد، عناوين حقول النموذج |
| `--text-lg` | 18px / 26px | 600 | عنوان بطاقة |
| `--text-xl` | 20px / 28px | 700 | عنوان قسم |
| `--text-2xl` | 24px / 32px | 700 | عنوان صفحة |
| `--text-3xl` | 30px / 38px | 800 | رقم KPI متوسط |
| `--text-4xl` | 36px / 44px | 800 | رقم KPI رئيسي (إجمالي المبيعات) |

**قاعدة الأرقام:** أي رقم (مبلغ، عدد طلبات، وقت) يُغلَّف بـ `direction: ltr; unicode-bidi: isolate` ويُعرض بالخط نفسه (Cairo يدعم الأرقام اللاتينية بشكل ممتاز) — لا يتحول اتجاهه أبدًا حتى داخل جملة عربية.

**Letter-spacing:** `-0.01em` على العناوين (`text-xl` فأعلى) لضغط الحروف العربية المتصلة بصريًا، `0` على النص العادي، `0.02em` على `text-xs` الأحرف الكبيرة (labels).

---

## 3. الشبكة والمسافات (Spacing — شبكة 8px صارمة)

كل قيمة مضاعف لـ 4px. لا استثناءات، لا قيم "تبدو حلوة" مثل 15px أو 18px.

```
--space-0-5: 2px   --space-4: 16px   --space-10: 40px
--space-1:   4px   --space-5: 20px   --space-12: 48px
--space-2:   8px   --space-6: 24px   --space-16: 64px
--space-3:  12px   --space-8: 32px   --space-20: 80px
```

**قواعد الاستخدام:**
- الحشو الداخلي لعناصر تفاعلية (زر، حقل): `--space-2` عموديًا × `--space-4` أفقيًا (md)، `--space-1-5`×`--space-3` (sm).
- الفجوة بين عناصر متجاورة في نفس المجموعة: `--space-2` أو `--space-3`.
- الفجوة بين أقسام منفصلة: `--space-6` أو `--space-8`.
- حواف الصفحة على الموبايل: `--space-4`؛ على سطح المكتب: `--space-8`.

### نقاط الانكسار (Mobile-First)

| Token | العرض | السياق |
|---|---|---|
| Base | 0px | موبايل — عمود واحد، تنقل سفلي |
| `--bp-md` | 640px | تابلت — عمودان، شريط جانبي مطوي |
| `--bp-lg` | 1024px | سطح المكتب — شريط جانبي ثابت 256px |
| `--bp-xl` | 1280px | لوحة تحكم كاملة — أقصى عرض للمحتوى 1280px |

---

## 4. الأشكال والارتفاع (Radius / Elevation)

نصف قطر صغير مقصود — الاستدارة الكاملة تقرأ كـ"استهلاكي"، الحدة الزائدة تقرأ كـ"بارد". التوازن المؤسسي بينهما.

| Token | القيمة | الاستخدام |
|---|---|---|
| `--radius-xs` | 4px | Checkbox، مؤشرات صغيرة |
| `--radius-sm` | 6px | حقول الإدخال، أزرار صغيرة |
| `--radius-md` | 8px | أزرار، بطاقات فرعية |
| `--radius-lg` | 10px | بطاقات رئيسية، نوافذ منبثقة صغيرة |
| `--radius-xl` | 12px | لوحات كبيرة، Modal |
| `--radius-full` | 999px | Avatar، نقطة حالة، شارة Pill |

### الظلال — الحدود أولاً، الظل استثناء

```
--shadow-xs: 0 1px 2px rgba(15,23,42,.04)              /* بطاقة ساكنة */
--shadow-sm: 0 1px 3px rgba(15,23,42,.06), 0 1px 2px rgba(15,23,42,.04)  /* بطاقة قابلة للتحويم */
--shadow-md: 0 4px 6px rgba(15,23,42,.05), 0 2px 4px rgba(15,23,42,.04)  /* قائمة منسدلة */
--shadow-lg: 0 10px 15px rgba(15,23,42,.06), 0 4px 6px rgba(15,23,42,.04) /* Modal */
--shadow-focus: 0 0 0 3px rgba(79,70,229,.15)           /* حلقة التركيز — بديل outline */
```

**قاعدة:** البطاقات الساكنة تستخدم `border` فقط بلا ظل. الظل يظهر فقط عند طبقة فوق طبقة (Dropdown, Modal, Popover) أو عند تفاعل (hover على صف قابل للسحب).

---

## 5. الحركة (Motion)

لا Bounce، لا Spring. `ease-out` أو `linear` فقط — الحركة المؤسسية تخدم الوظيفة لا المتعة.

| Token | المدة | Easing | الاستخدام |
|---|---|---|---|
| `--motion-instant` | 100ms | linear | Hover لون/خلفية |
| `--motion-fast` | 150ms | ease-out | ضغط زر، تبديل Tab |
| `--motion-base` | 200ms | ease-out | فتح Dropdown/Popover |
| `--motion-modal` | 240ms | ease-out | فتح/إغلاق Modal (مع تراجع خلفي 150ms) |

`prefers-reduced-motion: reduce` → تعطيل كل انتقال باستثناء تغييرات اللون الفورية.

---

## 6. المكونات (Component Specifications)

### 6.1 الأزرار

أربعة أنماط فقط: **Primary / Secondary / Ghost / Destructive**. ثلاثة أحجام: **sm (32px) / md (36px) / lg (44px)** ارتفاع كلي.

| النمط | الخلفية | النص | الحدود |
|---|---|---|---|
| Primary | `--color-primary` | أبيض | بلا |
| Secondary | `--color-surface` | `--color-text` | `1px solid --color-border-strong` |
| Ghost | شفاف | `--color-text-secondary` | بلا (خلفية `--color-surface-sunken` عند hover) |
| Destructive | `--color-danger-tint` | `--color-danger` | `1px solid` بلون danger بشفافية 20% |

الحشو: `sm`: 6px 12px / `md`: 8px 16px / `lg`: 10px 20px. نصف القطر: `--radius-sm` (sm/md)، `--radius-md` (lg).
حالة التركيز: `--shadow-focus` + بلا outline افتراضي المتصفح. حالة التعطيل: شفافية 45% + `cursor: not-allowed`، بلا تغيير لون.

### 6.2 حقول الإدخال

ارتفاع 36px (md) قياسي، 40px للنماذج الحرجة (تسجيل الدخول). خلفية `--color-surface`، حدود `1px solid --color-border`. عند التركيز: حدود `--color-primary` + `--shadow-focus`. عند الخطأ: حدود `--color-danger` + رسالة أسفل الحقل بـ `--text-xs` بلون danger، أيقونة تحذير داخل الحقل (يمين الحقل في RTL = البداية البصرية اليسرى... تُستخدم `inset-inline-end`).

التسمية (Label) دائمًا فوق الحقل، `--text-sm` وزن 500، لون `--color-text-secondary`. لا Placeholder كبديل عن Label أبدًا.

### 6.3 الشارات وحالات الطلب (Badges / Status)

Pill بارتفاع 20-24px، `--text-xs` وزن 600، حشو 2px 8px، خلفية Tint + نص باللون الأساسي للحالة، بلا حدود.

خريطة حالات الطلب (الأساس التشغيلي لدكان):

| الحالة | اللون | التسمية العربية |
|---|---|---|
| `pending` | Warning | قيد الانتظار |
| `preparing` | Info | قيد التحضير |
| `ready` | Success (tint فاتح) | جاهز |
| `completed` | `--color-text-muted` bg `--color-surface-sunken` | مكتمل |
| `cancelled` | Danger | ملغى |

### 6.4 البطاقات

`--color-surface` + `1px solid --color-border` + `--radius-lg`. حشو داخلي `--space-4` (موبايل) / `--space-6` (سطح مكتب). لا ظل افتراضيًا؛ `--shadow-sm` فقط عند إمكانية النقر/السحب (Kanban).

### 6.5 بطاقة KPI (لوحة التحليلات)

```
┌─────────────────────────────┐
│ إجمالي المبيعات اليوم   ↑12%│  ← label text-sm/secondary + trend badge
│                              │
│ 1,284.500 د.ب                │  ← text-4xl/800, أرقام LTR
└─────────────────────────────┘
```
اتجاه المؤشر (↑/↓) بلون success/danger فقط على الرقم، لا على خلفية البطاقة كاملة.

### 6.6 الجداول

صف بارتفاع 44px (كثيف، مؤسسي). رأس الجدول: `--text-xs` وزن 600 uppercase-style (استخدام tracking بدل uppercase الفعلي لعدم كسر العربية)، خلفية `--color-surface-sunken`، حدود سفلية `--color-border`. الصفوف: حدود سفلية 1px فقط بين الصفوف (لا حدود عمودية). Hover: خلفية `--color-surface-sunken`. صف محدد: خلفية `--color-primary-tint` + حد جانبي (inline-start) 2px بلون primary.

### 6.7 التنقل

- **موبايل (أساسي):** شريط سفلي ثابت 4-5 عناصر، ارتفاع 56px + safe-area، أيقونة + تسمية `--text-xs`. العنصر النشط بلون primary، الباقي `--color-text-muted`.
- **سطح المكتب (≥1024px):** شريط جانبي ثابت 256px، خلفية `--color-surface`، حدود `inline-end`. العنصر النشط: خلفية `--color-primary-tint`، نص `--color-primary`، وزن 600، بلا حد جانبي ملون (الخلفية كافية = هدوء بصري).

### 6.8 تذكرة المطبخ (KDS — لوحة تحضير الطلبات)

سطح **فاتح** موحّد مع بقية النظام (لا استثناء داكن — قرار المنتج). تباين عالٍ للقراءة السريعة عن بعد:
- بطاقة بحدود `2px` (أثخن من الافتراضي) للتمييز عن مسافة، لون الحد = لون حالة الطلب.
- رقم الطاولة/الطلب بـ `--text-2xl` وزن 800 أعلى البطاقة.
- المؤقت (وقت الانتظار) بلون يتدرج تلقائيًا: أخضر (`success`) → كهرمان (`warning`) → أحمر (`danger`) عند تجاوز عتبات زمنية محددة (5 د / 12 د / 20 د)، دون وميض أو حركة — التغيير في اللون كافٍ.
- أزرار الإجراء (بدء التحضير / جاهز) بحجم `lg` دائمًا — تُستخدم بأصابع سريعة في بيئة مطبخ.

### 6.9 التنبيهات (Alerts / Toast)

Alert مضمّن (Banner) بعرض كامل: خلفية Tint + حد `inline-start` 3px بلون الحالة + أيقونة + نص. Toast عائم: `--color-surface` + `--shadow-lg` + حد رفيع، يظهر أعلى الشاشة في RTL يبدأ من الجهة العلوية اليمنى منطقيًا (`inset-inline-end`)، يختفي تلقائيًا بعد 4 ثوانٍ إلا للأخطاء (يبقى حتى الإغلاق اليدوي).

---

## 7. قواعد RTL الإلزامية

1. **خصائص منطقية فقط:** `margin-inline-start/end`, `padding-inline-*`, `inset-inline-*`, `border-inline-*` — ممنوع `left`/`right` في CSS الجديد.
2. **الأيقونات الاتجاهية تُعكس:** أسهم، شيفرون، أيقونة "رجوع"، أيقونة "إرسال". **لا تُعكس:** أيقونة بحث، ساعة، سلة، علامة صح/خطأ، شعار.
3. **الأرقام والعملة LTR دائمًا** (راجع §2). رمز العملة (`د.ب`) يأتي **بعد** الرقم مع مسافة، ككتلة واحدة معزولة اتجاهيًا.
4. **المخططات (Charts):** محور X يبدأ من اليمين (الزمن يتقدم يمين→يسار) في `analytics`، مع الحفاظ على القيم الرقمية LTR داخل التلميحات (tooltips).
5. **حقول الإدخال اللاتينية** (بريد إلكتروني، رابط) تبقى `dir="ltr"` بمحاذاة نص تبدأ من اليسار حتى داخل نموذج RTL.

---

## 8. إمكانية الوصول (الحد الأدنى غير القابل للتفاوض)

- تباين نص/خلفية ≥ 4.5:1 لكل نص أساسي، ≥ 3:1 للنص الكبير (`text-xl`+).
- حلقة تركيز مرئية (`--shadow-focus`) على كل عنصر تفاعلي — لا `outline: none` بدون بديل.
- مساحة لمس دنيا 40×40px على الموبايل حتى لو كان العنصر البصري أصغر (padding شفاف).
- `prefers-reduced-motion` محترم في كل مكون (§5).

---

## 9. ما تم استبعاده عمدًا

- **لا Dark Mode.** قرار منتج نهائي — يبسّط الاختبار، يوحّد الهوية، ويناسب بيئة استخدام نهارية (كاشير/مطبخ مضاء).
- **لا خطوط عربية إضافية** (Tajawal, El Messiri, Noto سبق تجربتها — تم التوحيد على Cairo فقط لضبط الاتساق).
- **لا ألوان زخرفية إضافية** (ذهبي/عنبر كلون علامة تجارية) — اللون الوحيد غير المحايد هو Indigo، والباقي دلالي بحت.
- **لا Gradient، لا Glassmorphism، لا حركات دخول متتالية (stagger animations).**

---

## 10. استثناءات هوية طرف ثالث (موثّقة — ليست مخالفات)

استثناءات **مقصودة** لنظام التوكينات العام، لا تُعدّ مخالفة في أي تدقيق قادم. الأساس المنطقي الموحّد: الألوان/الأنماط التي تحمل هوية علامة تجارية خارجية معروفة تُحفظ كما هي ليكون التعرف الفوري ممكنًا (نفس منطق زر واتساب الأخضر أو شعار جوجل بألوانه الأصلية).

### 10.1 ربط تيليجرام (`src/components/telegram-manager.tsx`)
أزرق تيليجرام `sky-100/200/50/600/700/800` + `bg-white` على بطاقات إعداد ربط الحساب — هوية بصرية لتطبيق تيليجرام تحديدًا، تمكّن المستخدم من التعرف الفوري على القسم المرتبط بالبوت. **لا يُستبدل** بتوكينات النظام.

### 10.2 طباعة QR للطاولات (`src/app/dashboard/tables/tables-client.tsx`)
ألوان hex مضمّنة داخل سلسلة HTML مستقلة للطباعة (`#0F172A`, `#E2E8F0`, `#475569`) — لا تمر على Tailwind إطلاقًا ولا على نظام التوكينات؛ استبدالها بتوكينات CSS غير ممكن/عديم القيمة عمليًا.

### 10.3 شاشة خطأ الطوارئ (`src/app/global-error.tsx`)
ألوان مضمّنة inline (`#F8FAFC`, `#0F172A`, `#4F46E5`, `#475569`) — صفحة fallback تعمل بالضبط عندما يفشل تحميل التطبيق/CSS نفسه؛ فرض توكينات يفترض تحميلًا ناجحًا، عكس الغرض منها تمامًا.
